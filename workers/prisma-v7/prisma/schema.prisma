generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  runtime      = "cloudflare"
  moduleFormat = "esm"
}

datasource db {
  provider = "postgresql"
}

model Hello {
  id      String @id
  content String
}
