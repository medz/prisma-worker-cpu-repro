{
  "name": "@repro/prisma-next-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "emit": "prisma-next contract emit prisma/schema.psl -o .prisma",
    "build": "tsc --noEmit && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260505.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.87.0"
  }
}
